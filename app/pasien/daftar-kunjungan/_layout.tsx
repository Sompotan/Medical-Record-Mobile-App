import {Slot, Stack} from "expo-router";
import {View} from "react-native";

export default function DaftarKunjunganLayout() {
    return (
        <Stack screenOptions={{
            headerShown: false,
        }}/>
    )
}